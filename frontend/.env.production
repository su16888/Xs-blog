# ========================================
# Xs-Blog 前端环境变量配置
# ========================================

# 前端服务端口
# PORT=3000

# ========================================
# API 配置（支持多种配置方式）
# ========================================
# 配置优先级（从高到低）：
# 1. NEXT_PUBLIC_API_URL（完整 URL）
# 2. NEXT_PUBLIC_API_HOST + PORT + PROTOCOL（拆分配置）
# 3. NEXT_PUBLIC_BACKEND_PORT（自动检测模式，使用当前域名）
# 4. 默认值（localhost:3001）

# 方式一：完整 URL（推荐，最简单）
# 适用场景：前后端分离部署，后端地址固定
# NEXT_PUBLIC_API_URL=http://localhost:3001/api

# 方式二：拆分配置（灵活性高）
# 适用场景：需要分别配置协议、主机、端口
# NEXT_PUBLIC_API_HOST=localhost
# NEXT_PUBLIC_API_PORT=3001
# NEXT_PUBLIC_API_PROTOCOL=http

# 方式三：自动检测模式（前后端同服务器部署）
# 只需指定后端端口，自动使用当前访问的域名
# 适用场景：前后端部署在同一服务器，使用不同端口
# NEXT_PUBLIC_BACKEND_PORT=3001

# 方式四：服务端渲染降级配置（可选）
# 用于服务端渲染时的默认配置
# NEXT_PUBLIC_DEFAULT_HOST=localhost
# NEXT_PUBLIC_DEFAULT_PROTOCOL=http

# ========================================
# 配置示例
# ========================================

# 示例 1：本地开发（默认）
# NEXT_PUBLIC_API_URL=http://localhost:3001/api

# 示例 2：前后端同服务器，不同端口
# NEXT_PUBLIC_BACKEND_PORT=3001
# （前端会自动使用当前域名 + 3001 端口）

# 示例 3：前后端分离，后端在其他服务器
# NEXT_PUBLIC_API_URL=http://192.168.1.100:3001/api

# 示例 4：生产环境，使用域名
# NEXT_PUBLIC_API_URL=https://api.yourdomain.com/api

# 示例 5：拆分配置方式
# NEXT_PUBLIC_API_HOST=api.yourdomain.com
# NEXT_PUBLIC_API_PORT=443
# NEXT_PUBLIC_API_PROTOCOL=https

# ========================================
# 使用建议
# ========================================
# 1. 本地开发：使用方式一（完整 URL）
# 2. 前后端同服务器：使用方式三（自动检测）
# 3. 前后端分离：使用方式一（完整 URL）
# 4. 需要灵活配置：使用方式二（拆分配置）
# 5. 如果需要使用反向代理（如 Nginx），可配置为相对路径
#    例: NEXT_PUBLIC_API_URL=/api
#        PORT=3000
# 但如果使用这个部署方式，请确保后端.env文件CORS_ORIGIN配置项填写了相关域名或IP
# ========================================
