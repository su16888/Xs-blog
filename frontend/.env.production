# 生产环境配置

# ========================================
# 🎯 部署模式选择（四选一，按需取消注释）
# ========================================

# ----------------------------------------
# 模式一：自动识别模式（推荐新手使用）
# ----------------------------------------
# 适用场景：前后端部署在同一服务器，端口不同（前端3000，后端3001）
# 工作原理：自动使用当前访问的域名/IP + 后端端口
# 例如：访问 http://123.4.5.6:3000 → 自动调用 http://123.4.5.6:3001/api
# 例如：访问 http://example.com:3000 → 自动调用 http://example.com:3001/api
# 配置方式：注释掉 NEXT_PUBLIC_API_URL 即可
# NEXT_PUBLIC_API_URL=
# NEXT_PUBLIC_BACKEND_PORT=3001

# ----------------------------------------
# 模式二：Nginx 反向代理模式（推荐生产环境）
# ----------------------------------------
# 适用场景：前端和后端通过 Nginx 统一入口访问（隐藏端口）
# Nginx 配置示例：
#   location /api {
#     proxy_pass http://127.0.0.1:3001;
#   }
# 配置方式：使用相对路径
NEXT_PUBLIC_API_URL=/api

# ----------------------------------------
# 模式三：前后端分离模式（跨域/跨服务器）
# ----------------------------------------
# 适用场景：前端和后端部署在不同服务器或不同域名
# 配置方式：使用完整 URL
# NEXT_PUBLIC_API_URL=http://api.example.com:3001/api
# NEXT_PUBLIC_API_URL=https://api.yourdomain.com/api

# ----------------------------------------
# 模式四：拆分配置模式（高级）
# ----------------------------------------
# 适用场景：需要分别配置协议、主机、端口
# NEXT_PUBLIC_API_HOST=api.example.com
# NEXT_PUBLIC_API_PORT=3001
# NEXT_PUBLIC_API_PROTOCOL=https

# ========================================
# 前端服务端口
# ========================================
PORT=3000
