/**
 * Xs-Blog 前端运行时配置示例
 *
 * 使用方法：
 * 1. 复制此文件并重命名为 config.js
 * 2. 根据您的部署环境修改配置
 * 3. 刷新浏览器即可生效，无需重新构建！
 *
 * ========================================
 * 配置说明
 * ========================================
 */
window.__RUNTIME_CONFIG__ = {

  // ========================================
  // API 地址配置（四选一，取消注释您需要的方式）
  // ========================================

  // ----------------------------------------
  // 方式一：Nginx 反向代理模式（推荐生产环境）
  // ----------------------------------------
  // 适用场景：前端和后端通过 Nginx 统一入口访问（隐藏端口）
  // Nginx 配置示例：
  //   server {
  //     listen 80;
  //     server_name yourdomain.com;
  //
  //     location / {
  //       proxy_pass http://127.0.0.1:3000;
  //     }
  //
  //     location /api {
  //       proxy_pass http://127.0.0.1:3001;
  //     }
  //   }
  API_URL: '/api',

  // ----------------------------------------
  // 方式二：前后端分离模式（跨域/跨服务器）
  // ----------------------------------------
  // 适用场景：前端和后端部署在不同服务器或不同域名
  // 注意：需要在后端配置 CORS 允许前端域名
  //
  // API_URL: 'http://192.168.1.100:3001/api',
  // API_URL: 'http://api.example.com:3001/api',
  // API_URL: 'https://api.yourdomain.com/api',

  // ----------------------------------------
  // 方式三：同服务器直连模式
  // ----------------------------------------
  // 适用场景：前后端部署在同一服务器，通过不同端口访问
  //
  // API_URL: 'http://localhost:3001/api',

  // ----------------------------------------
  // 方式四：自动检测模式（推荐新手使用）
  // ----------------------------------------
  // 适用场景：前后端部署在同一服务器，端口不同
  // 工作原理：自动使用当前访问的域名/IP + 后端端口
  // 例如：访问 http://123.4.5.6:3000 → 自动调用 http://123.4.5.6:3001/api
  // 配置方式：将 API_URL 设置为空字符串
  //
  // API_URL: '',

  // ========================================
  // 后端端口配置（仅自动检测模式使用）
  // ========================================
  // 当 API_URL 为空时，系统会自动使用当前域名 + 此端口
  BACKEND_PORT: '3001',

};
